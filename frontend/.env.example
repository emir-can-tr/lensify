# Lensify Frontend Environment Configuration

# Backend API URL - Change this if your backend runs on a different port or domain
VITE_API_URL=http://localhost:8000

# Development settings
VITE_DEV_MODE=true

# Optional: Analytics or tracking (uncomment if needed)
# VITE_ANALYTICS_ID=your_analytics_id

# Optional: Sentry error tracking (uncomment if needed)  
# VITE_SENTRY_DSN=your_sentry_dsn

# Optional: Custom branding (uncomment and modify if needed)
# VITE_APP_NAME=Lensify
# VITE_APP_DESCRIPTION=Instant Photo Effects
