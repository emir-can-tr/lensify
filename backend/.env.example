# Lensify Backend Environment Configuration

# Server configuration
HOST=0.0.0.0
PORT=8000

# Development settings
DEBUG=true
RELOAD=true

# CORS origins - Add your frontend URLs here
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173

# File upload settings
MAX_FILE_SIZE=10485760  # 10MB in bytes
ALLOWED_EXTENSIONS=jpg,jpeg,png,gif,bmp,webp

# Image processing settings
MAX_IMAGE_DIMENSION=4096
JPEG_QUALITY=95

# Optional: Logging configuration
LOG_LEVEL=INFO
LOG_FILE=lensify.log

# Optional: Rate limiting (requests per minute per IP)
RATE_LIMIT=100

# Optional: API key for premium features (leave empty for open access)
API_KEY=

# Optional: Database configuration (if you add user accounts later)
# DATABASE_URL=sqlite:///lensify.db
